# BSH 暖碟抽屉 故障诊断索引 V1.0

---

## 一、使用说明

### 本索引用途

本索引是 LLM 执行 暖碟抽屉 故障诊断的**一级分诊入口**。当用户描述故障现象时，LLM 应首先查阅本索引的“一级分流表”，定位到对应的故障树文件，然后加载该文件执行诊断。

### 文件体系说明

- **本文件**（索引）：`BSH_WDW_V1.0_index.md` — 分流入口，不含具体诊断逻辑
- **独立故障树文件**：`BSH_WDW_F{nn}_{故障关键词}.md` — 每棵故障树的完整诊断手册

### 分流执行方法

1. 用户描述故障现象
2. 在“一级分流表”中匹配关键词，确定目标故障树文件
3. 加载对应文件，从该文件的“诊断入口”开始执行
4. 若诊断过程中遇到跨树引用（`【引用】`），切换到目标文件继续

### 三条执行铁律

1. **不越界**：只执行当前加载的故障树文件中定义的诊断步骤，遇到超出范围的情况执行越界协议
2. **不编造**：话术原文不得改写、补充或省略；不在文件中的诊断码不得使用
3. **不跳步**：严格按 D 步骤顺序执行，不跳过中间步骤直接给出结论

---

## 二、文件清单

| 编号 | 文件名 | 故障类型 | Mermaid 节点前缀 | PPT 来源页 |
| --- | --- | --- | --- | --- |
| F01 | `BSH_WDW_F01_不通电.md` | 不通电 | `NP` | Slide 2 |
| F02 | `BSH_WDW_F02_按键异常.md` | 按键异常 | `BT` | Slide 3 |
| F03 | `BSH_WDW_F03_不工作.md` | 不工作 | `NWK` | Slide 4 |
| F04 | `BSH_WDW_F04_按键异常.md` | 按键异常 | `BT2` | Slide 5 |
| F05 | `BSH_WDW_F05_不工作.md` | 不工作 | `NWK2` | Slides 6-7 |

**被过滤的非流程页**：Slide 1（封面/标题页）、Slide 8（版本记录页）

---

## 三、一级分流表

| 用户描述关键词 | 分流到文件 | 备注 |
| --- | --- | --- |
| 不通电、暖碟抽屉故障树、请问您是触摸按键、还是旋钮档位的产、不工作、通电不加热 | F01 不通电 | 自动抽取关键词，待人工扩充同义词 |
| 按键异常、暖碟抽屉故障树、A1100、不加热、请问您是触摸按键、还是旋钮档位的产 | F02 按键异常 | 自动抽取关键词，待人工扩充同义词 |
| 不工作、暖碟抽屉故障树、A1100、不加热、可能选择档位过低、或摆放的餐具过多 | F03 不工作 | 自动抽取关键词，待人工扩充同义词 |
| 按键异常、暖碟抽屉故障树、E0000、Y25、电源灯闪烁、不工作 | F04 按键异常 | 自动抽取关键词，待人工扩充同义词 |
| 不工作、暖碟抽屉故障树、部件损坏、请问是抽屉前面板、还是、抽屉底板损坏 | F05 不工作 | 自动抽取关键词，待人工扩充同义词 |

---

## 四、跨故障树引用关系

| 来源文件 | 引用目标文件 | 触发条件 |
| --- | --- | --- |
| 无明确跨故障树引用 | — | — |

---

## 五、产品系列与适用机型

- **产品系列代码**：`WDW`（Warming Drawer，自动推断）
- **来源文档**：`GGN_暖碟(烹饪)抽屉故障树_V1.0.pptx`
- **文档版本**：V1.0
- **适用机型**：暖碟抽屉 相关机型
- **知识手册生成日期**：2026-05-22
- **转换状态**：自动批处理草案，需按源 PPT 视觉关系复核
